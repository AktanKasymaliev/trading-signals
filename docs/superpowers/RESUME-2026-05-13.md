# Resume Point — 2026-05-13

> Self-note: read this FIRST after context clear. Then read the plan file linked below.

## TL;DR

Working on AI layer for XAU/USD signal bot. Branch `feature/hugging-face-ai-layer`.
Critical fixes done. PoC with HF model SMC v2 done. Calibration diagnostic done.
**Next: implement Path C — train own LightGBM model.**

## Branch state

- Branch: `feature/hugging-face-ai-layer` (pushed to origin)
- 16 commits since `15ca69c`. Working tree has 4 untracked items (see below).
- All 156 tests pass.

## What's been completed

### A. Critical AI-layer fixes (8 commits ending `2526dbd`)
- AI_MODEL_REVISION, multi-class normalization, NaN policy, instance caching
- formatter AI line, backtest `--compare-ai`, README
- See `docs/HANDOFF.md` end-of-file for details

### B. SMC v2 PoC integration
Plan: `docs/superpowers/plans/2026-05-12-poc-smc-v2-integration.md`
Outcome: infrastructure works end-to-end. Model loads, predicts, calibrates.
But on 2-month window baseline produced 0 signals (lookback issue — D1/W1 EMAs were NaN).
See HANDOFF.md "PoC: SMC v2" section.

### C. Calibration diagnostic on 3-year data (today)
- Downloaded `ZombitX64/xauusd-gold-price-historical-data-2004-2025` via HF.
- Created `./data_long_m15.csv` (80K M15 bars, 2022-2025).
- Ran baseline backtest: **293 signals, WR 29.3%, E=-0.07R, PF=0.84 → losing strategy.**
- AI didn't help — never overrode existing signals, blocked only NO_SIGNAL candidates.
- Per-tier: NORMAL WR 38.5% (13 trades), WEAK WR 15.3% (59 trades). WEAK is garbage.
- **Conclusion:** Calibration mechanics work, but baseline strategy itself loses on real data.
- **User decision:** skip B (NaN-tolerance, low priority now) + skip tuning. Go straight to Path C.

## Next: Path C — own LightGBM

Plan: `docs/superpowers/plans/2026-05-13-path-c-lightgbm.md`

### Architecture
- Use OUR `build_ai_features` (29 features, `feature_set=internal`)
- Labels: forward 4h return (16 M15 bars). +1 BUY if return > +0.3%, -1 SELL if < -0.3%, else 0
- Train LightGBM 3-class classifier, time-based 80/20 split
- Save to `./models_cache/path_c_lgb.joblib` (local file, not HF)
- Add `AI_MODEL_LOCAL_PATH` env var so adapter loads from disk, skipping HF download
- Run `--compare-ai` with this local model on long history

### Tasks (5)
| # | What | Status |
|---|---|---|
| C1 | `AI_MODEL_LOCAL_PATH` config + adapter bypass + tests | in_progress (was about to dispatch subagent when interrupted) |
| C2 | Add `lightgbm>=4.0,<5.0` to requirements.txt | pending |
| C3 | `xau_pro_bot/models/train_lightgbm.py` + tests | pending |
| C4 | `scripts/train_path_c_model.py` + actual training run | pending |
| C5 | `scripts/poc_path_c_compare.py` + record findings | pending |

### Where C1 left off
- File `tests/test_local_model_path.py` ALREADY EXISTS (41 lines) — created during earlier dispatch attempt. **Check its contents before re-dispatching** — it may already have the 3 spec tests, in which case no need to rewrite.
- Code changes NOT yet applied: `config.py` (`local_path` key), `hf_model.py` (`local_path` kwarg + early-return in `_load_sklearn`), `engine.py` (plumb `local_path`), `.env.example` (append `AI_MODEL_LOCAL_PATH=`).
- C1 prompt is in the plan file — just dispatch from the plan directly.

## Untracked files in working tree

- `data_long_m15.csv` — 3y M15 history (gitignored — too big for repo)
- `docs/superpowers/plans/2026-05-12-poc-smc-v2-integration.md` — SMC v2 plan (already executed, can commit if desired)
- `docs/superpowers/plans/2026-05-13-path-c-lightgbm.md` — **Path C plan, executing this**
- `tests/test_local_model_path.py` — partial C1 artifact, see "Where C1 left off"

`.gitignore` already excludes `data_xauusd_*.csv` and `models_cache/` (added in commit `2526dbd`). `data_long_m15.csv` is NOT yet gitignored — add it if committing later. (Or rename it to `data_xauusd_long_m15.csv` to match the existing pattern.)

## Execution mode

Subagent-Driven Development per `superpowers:subagent-driven-development`:
- One implementer subagent per task (general-purpose, sonnet model)
- Spec reviewer (haiku) after each
- Mark task in_progress before dispatch, completed after review approves

## Key env vars reference

| Var | Purpose |
|---|---|
| `AI_ENABLED` | true/false toggle |
| `AI_MODEL_ID` | HF repo id (ignored when `AI_MODEL_LOCAL_PATH` set) |
| `AI_MODEL_REVISION` | 40-char commit SHA (required for HF sklearn) |
| `AI_MODEL_FILENAME` | Override default artifact filename |
| `AI_MODEL_TYPE` | sklearn / transformers / custom |
| `AI_FEATURE_SET` | internal (default, 29 fich) / smc_v2 (21 fich) |
| `AI_MODEL_LOCAL_PATH` | **NEW (C1):** local joblib path, bypasses HF |
| `AI_CACHE_DIR` | HF download cache |

## Important constraints from user

- **Speak Russian by default** (CLAUDE.md). Brief, no waffle.
- **Save tokens. Be ECO.** User asked explicitly.
- Don't commit without explicit permission.
- Don't push without explicit permission.
- Don't modify linter configs to silence errors — fix code.
- Don't auto-spawn subagents excessively.

## Resume command after clear

Read this file → read `docs/superpowers/plans/2026-05-13-path-c-lightgbm.md` → check `tests/test_local_model_path.py` contents → resume from C1 dispatch (or skip ahead if C1 already partially done).
